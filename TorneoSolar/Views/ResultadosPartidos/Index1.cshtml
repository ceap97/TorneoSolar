@model IEnumerable<TorneoSolar.Models.ResultadosPartido>

@{
    ViewData["Title"] = "Resultados de Partidos";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<div class="container mt-4">
    <h2 class="text-primary">🏀 Resultados de Partidos</h2>

    <div class="d-flex justify-content-between align-items-center mb-3">
        <p class="text-muted">Consulta los resultados de los partidos.</p>
        <a asp-action="Create" class="btn btn-success btn-sm">➕ Crear Nuevo</a>
    </div>

    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark text-center">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.PuntosLocal)</th>
                    <th>@Html.DisplayNameFor(model => model.PuntosVisitante)</th>
                    <th>@Html.DisplayNameFor(model => model.Partido)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody class="text-center align-middle">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.PuntosLocal)</td>
                        <td>@Html.DisplayFor(modelItem => item.PuntosVisitante)</td>
                        <td>@Html.DisplayFor(modelItem => item.Partido.PartidoId)</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.ResultadoId" class="btn btn-warning btn-sm">✏️ Editar</a>
                            <a asp-action="Details" asp-route-id="@item.ResultadoId" class="btn btn-info btn-sm">👀 Detalles</a>
                            <a asp-action="Delete" asp-route-id="@item.ResultadoId" class="btn btn-danger btn-sm">🗑️ Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="mt-3">
        <a asp-action="Index" class="btn btn-outline-secondary">⬅️ Volver al Inicio</a>
    </div>
</div>
